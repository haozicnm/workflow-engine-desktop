# v2.0 ComfyUI 风格编辑器 — 实现计划

> **For Hermes:** Use `subagent-driven-development` skill to implement tasks.
> **设计文档:** [comfyui-style-design-vision.md](comfyui-style-design-vision.md)
> **版本路线:** [task_plan.md](../task_plan.md)
> **创建:** 2026-04-28

## 目标

将现有代码驱动的线性步骤编辑器，改造为 ComfyUI 风格的数据流可视化节点编辑器。
用户拖节点、填参数、连线，点运行即可。

## 架构

```
前端 (Vue 3 + Vue Flow)                    后端 (Rust / Tauri)
───────────────────────                    ─────────────────────
Vue Flow 画布 (无限缩放/平移)               DAG 解析 (拓扑排序)
├── 自定义节点 (ComfyUI 风格 CSS)            ├── 节点依赖图构建
├── 针脚系统 (6 色 + 类型安全)              └── 并行组检测
├── 连线/拖拽                               
├── 节点库面板 (搜索 + 拖入)                执行引擎
├── 属性面板 (右侧)                         ├── 逐个节点执行
└── 数据预览 (悬停气泡)                      ├── Tauri events 推送状态
                                           └── 数据在节点间传递
                                           
通信: Tauri invoke (操作) + Tauri events (状态推送)
```

**技术栈:** Vue 3 + TypeScript + Vue Flow + Pinia + Tailwind CSS v4 | Rust + Tauri 2.0

---

## Phase 1: 基础节点编辑器 (P1)

> 目标：Vue Flow 画布 + ComfyUI 风格节点 + 连线系统 + 节点库面板 + 4 个基础节点

### 前置准备

#### Task 1.1: 安装 Vue Flow 依赖

**Objective:** 添加 Vue Flow 到项目依赖

**Files:**
- Modify: `src/package.json`

**Step 1: 安装**

```bash
cd /home/horizon/workflow-engine-desktop/src
npm install @vue-flow/core @vue-flow/background @vue-flow/controls @vue-flow/minimap
```

**Step 2: 验证**

```bash
npm ls @vue-flow/core
# Expected: @vue-flow/core@^1.x
```

---

#### Task 1.2: 创建 DAG 节点类型定义

**Objective:** 定义数据流节点类型（替代旧的线性 Step 类型），包含输入/输出针脚、参数、状态

**Files:**
- Create: `src/types/dag-node.ts`

```typescript
// ─── 针脚类型 ───
export interface PinDefinition {
  id: string
  label: string
  type: PinType
  required?: boolean
}

export type PinType = 'string' | 'number' | 'boolean' | 'object' | 'array' | 'file' | 'any' | 'error'

export const PIN_TYPE_COLORS: Record<PinType, string> = {
  string: '#4A9EFF',   // 🔵 蓝
  number: '#4ADE80',   // 🟢 绿
  boolean: '#FBBF24',  // 🟡 黄
  object: '#FB923C',   // 🟠 橙
  array: '#C084FC',    // 🟣 紫
  file: '#9CA3AF',     // ⚪ 灰
  any: '#E5E7EB',      // ⬜ 白
  error: '#EF4444',    // 🔴 红
}

// ─── 节点定义 ───
export interface DAGNodeDefinition {
  type: string
  label: string
  icon: string
  color: string
  category: 'source' | 'process' | 'ai' | 'output'
  inputs: PinDefinition[]
  outputs: PinDefinition[]
  defaultConfig: Record<string, unknown>
}

// ─── 运行时节点 ───
export interface DAGNode<T = Record<string, unknown>> {
  id: string
  type: string
  label: string
  position: { x: number; y: number }
  config: T
  // 运行时状态
  status?: DAGNodeStatus
  output?: unknown
  error?: string
  duration?: number
}

export type DAGNodeStatus = 'idle' | 'queued' | 'running' | 'success' | 'error'

export const STATUS_COLORS: Record<DAGNodeStatus, string> = {
  idle: '#9CA3AF',
  queued: '#FBBF24',
  running: '#3B82F6',
  success: '#4ADE80',
  error: '#EF4444',
}

// ─── 连线 ───
export interface DAGEdge {
  id: string
  source: string        // node id
  target: string        // node id
  sourceHandle: string  // output pin id
  targetHandle: string  // input pin id
}

// ─── 工作流图 ───
export interface DAGWorkflow {
  name: string
  description?: string
  nodes: DAGNode[]
  edges: DAGEdge[]
  variables?: Record<string, unknown>
}

// ─── 基础节点注册表 ───
export const BASE_NODE_DEFINITIONS: DAGNodeDefinition[] = [
  {
    type: 'http',
    label: 'HTTP 请求',
    icon: '🌐',
    color: '#3B82F6',
    category: 'source',
    inputs: [
      { id: 'headers', label: 'headers', type: 'object' },
      { id: 'body', label: 'body', type: 'object' },
    ],
    outputs: [
      { id: 'result', label: 'result', type: 'object' },
    ],
    defaultConfig: { url: '', method: 'GET', timeout: 30 },
  },
  {
    type: 'json_parse',
    label: 'JSON 解析',
    icon: '🔀',
    color: '#A855F7',
    category: 'process',
    inputs: [
      { id: 'input', label: 'input', type: 'object', required: true },
    ],
    outputs: [
      { id: 'result', label: 'result', type: 'object' },
    ],
    defaultConfig: { expression: '$' },
  },
  {
    type: 'text_template',
    label: '文本拼接',
    icon: '📝',
    color: '#8B5CF6',
    category: 'process',
    inputs: [
      { id: 'data', label: 'data', type: 'object' },
    ],
    outputs: [
      { id: 'result', label: 'result', type: 'string' },
    ],
    defaultConfig: { template: '' },
  },
  {
    type: 'file_save',
    label: '保存文件',
    icon: '💾',
    color: '#9CA3AF',
    category: 'output',
    inputs: [
      { id: 'content', label: 'content', type: 'object', required: true },
    ],
    outputs: [],
    defaultConfig: { path: '', format: 'json' },
  },
]
```

---

#### Task 1.3: 创建 DAG Workflow Store (Pinia)

**Objective:** 状态管理：节点列表、连线列表、拖拽、撤销/重做

**Files:**
- Create: `src/stores/dag-store.ts`

```typescript
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import type { DAGNode, DAGEdge, DAGNodeStatus } from '../types/dag-node'

export const useDAGStore = defineStore('dag', () => {
  const nodes = ref<DAGNode[]>([])
  const edges = ref<DAGEdge[]>([])
  const nodeStatuses = ref<Record<string, DAGNodeStatus>>({})

  // ─── 节点操作 ───
  function addNode(node: DAGNode) {
    nodes.value.push(node)
    nodeStatuses.value[node.id] = 'idle'
  }

  function removeNode(id: string) {
    nodes.value = nodes.value.filter(n => n.id !== id)
    edges.value = edges.value.filter(e => e.source !== id && e.target !== id)
    delete nodeStatuses.value[id]
  }

  function updateNodePosition(id: string, position: { x: number; y: number }) {
    const node = nodes.value.find(n => n.id === id)
    if (node) node.position = position
  }

  function updateNodeConfig(id: string, config: Record<string, unknown>) {
    const node = nodes.value.find(n => n.id === id)
    if (node) node.config = { ...node.config, ...config }
  }

  // ─── 连线操作 ───
  function addEdge(edge: DAGEdge) {
    edges.value.push(edge)
  }

  function removeEdge(id: string) {
    edges.value = edges.value.filter(e => e.id !== id)
  }

  // ─── 状态操作 ───
  function setNodeStatus(id: string, status: DAGNodeStatus) {
    nodeStatuses.value[id] = status
  }

  function resetAllStatuses() {
    Object.keys(nodeStatuses.value).forEach(id => {
      nodeStatuses.value[id] = 'idle'
    })
  }

  // ─── 序列化/反序列化 ───
  const workflowJson = computed(() => ({
    nodes: nodes.value,
    edges: edges.value,
  }))

  function loadWorkflow(data: { nodes: DAGNode[]; edges: DAGEdge[] }) {
    nodes.value = data.nodes
    edges.value = data.edges
    data.nodes.forEach(n => {
      nodeStatuses.value[n.id] = 'idle'
    })
  }

  function clear() {
    nodes.value = []
    edges.value = []
    nodeStatuses.value = {}
  }

  return {
    nodes, edges, nodeStatuses,
    addNode, removeNode, updateNodePosition, updateNodeConfig,
    addEdge, removeEdge,
    setNodeStatus, resetAllStatuses,
    workflowJson, loadWorkflow, clear,
  }
})
```

---

### 核心组件

#### Task 2.1: 创建 ComfyUI 风格自定义节点

**Objective:** 渲染单个节点：标题栏（图标+名称+状态灯）、参数区、输入/输出针脚

**Files:**
- Create: `src/components/dag/ComfyNode.vue`

```vue
<template>
  <div
    class="comfy-node"
    :class="[`status-${status}`, { selected: selected }]"
    :style="{ borderColor: color }"
  >
    <!-- 状态指示灯 -->
    <div class="node-header" :style="{ backgroundColor: color + '18' }">
      <span class="status-dot" :style="{ backgroundColor: statusColor }"></span>
      <span class="node-icon">{{ icon }}</span>
      <span class="node-label">{{ label }}</span>
      <span v-if="duration" class="node-duration">{{ duration }}ms</span>
      <button class="node-run-btn" v-if="status === 'idle'" @click.stop="$emit('run-node')">▶</button>
    </div>

    <!-- 输入针脚 (左侧) -->
    <div class="pins-section inputs" v-if="inputs.length">
      <div
        v-for="pin in inputs"
        :key="pin.id"
        class="pin-row input-pin"
      >
        <Handle
          type="target"
          :id="pin.id"
          :position="Position.Left"
          :style="{ backgroundColor: getPinColor(pin.type) }"
        />
        <span class="pin-label">{{ pin.label }}</span>
        <span
          class="pin-type"
          :style="{ color: getPinColor(pin.type) }"
        >{{ pinTypeLabel(pin.type) }}</span>
      </div>
    </div>

    <!-- 参数区 -->
    <div class="node-params" v-if="hasParams">
      <slot name="params" />
    </div>

    <!-- 输出针脚 (右侧) -->
    <div class="pins-section outputs" v-if="outputs.length">
      <div
        v-for="pin in outputs"
        :key="pin.id"
        class="pin-row output-pin"
        @mouseenter="$emit('preview-pin', pin.id)"
        @mouseleave="$emit('hide-preview')"
      >
        <span class="pin-type" :style="{ color: getPinColor(pin.type) }">{{ pinTypeLabel(pin.type) }}</span>
        <span class="pin-label">{{ pin.label }}</span>
        <Handle
          type="source"
          :id="pin.id"
          :position="Position.Right"
          :style="{ backgroundColor: getPinColor(pin.type) }"
        />
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import { Handle, Position } from '@vue-flow/core'
import type { PinDefinition, DAGNodeStatus } from '../../types/dag-node'
import { PIN_TYPE_COLORS, STATUS_COLORS } from '../../types/dag-node'

const props = defineProps<{
  id: string
  label: string
  icon: string
  color: string
  status: DAGNodeStatus
  inputs: PinDefinition[]
  outputs: PinDefinition[]
  hasParams: boolean
  selected: boolean
  duration?: number
}>()

defineEmits<{
  'run-node': []
  'preview-pin': [pinId: string]
  'hide-preview': []
}>()

const statusColor = computed(() => STATUS_COLORS[props.status])
const getPinColor = (type: string) => PIN_TYPE_COLORS[type as keyof typeof PIN_TYPE_COLORS] || '#9CA3AF'
const pinTypeLabel = (type: string) => {
  const labels: Record<string, string> = {
    string: 'Aa', number: '#', boolean: '◉', object: '{}', array: '[]', file: '📄', any: '*', error: '!'
  }
  return labels[type] || type
}
</script>

<style scoped>
.comfy-node {
  width: 260px;
  background: #1e1e2e;
  border: 2px solid #3B82F6;
  border-radius: 8px;
  font-size: 13px;
  color: #cdd6f4;
  overflow: hidden;
  transition: box-shadow 0.2s;
}
.comfy-node.selected { box-shadow: 0 0 0 2px #89b4fa; }
.comfy-node.status-running { animation: breathe 1.5s ease-in-out infinite; }
@keyframes breathe {
  0%, 100% { box-shadow: 0 0 4px rgba(59, 130, 246, 0.3); }
  50% { box-shadow: 0 0 12px rgba(59, 130, 246, 0.6); }
}
.node-header {
  display: flex; align-items: center; gap: 6px;
  padding: 6px 10px; font-weight: 600; font-size: 12px;
}
.status-dot { width: 8px; height: 8px; border-radius: 50%; flex-shrink: 0; }
.node-duration { margin-left: auto; font-size: 11px; opacity: 0.6; }
.node-run-btn {
  margin-left: auto; width: 22px; height: 22px; border-radius: 50%;
  border: 1px solid #4ADE80; background: transparent; color: #4ADE80;
  cursor: pointer; font-size: 10px; display: flex; align-items: center; justify-content: center;
}
.node-run-btn:hover { background: #4ADE8022; }
.pins-section { padding: 4px 0; }
.pin-row {
  display: flex; align-items: center; gap: 6px; padding: 2px 8px;
  font-size: 11px; position: relative;
}
.input-pin { padding-left: 2px; }
.output-pin { justify-content: flex-end; padding-right: 2px; }
.pin-label { opacity: 0.8; }
.pin-type { font-family: monospace; font-size: 10px; font-weight: 600; min-width: 16px; text-align: center; }
.node-params { padding: 6px 10px; border-top: 1px solid #313244; }
</style>
```

---

#### Task 2.2: 创建节点库面板

**Objective:** 左侧面板，分类折叠显示可拖入的节点

**Files:**
- Create: `src/components/dag/NodeLibrary.vue`

```vue
<template>
  <div class="node-library">
    <div class="library-search">
      <input v-model="search" placeholder="🔍 搜索节点..." class="search-input" />
    </div>
    <div v-for="category in filteredCategories" :key="category.name" class="category">
      <div class="category-header" @click="category.expanded = !category.expanded">
        <span>{{ category.expanded ? '▼' : '▶' }}</span>
        <span>{{ category.name }}</span>
      </div>
      <div v-show="category.expanded" class="category-items">
        <div
          v-for="def in category.items"
          :key="def.type"
          class="library-item"
          :style="{ borderLeftColor: def.color }"
          draggable="true"
          @dragstart="onDragStart($event, def)"
        >
          <span class="item-icon">{{ def.icon }}</span>
          <span class="item-label">{{ def.label }}</span>
        </div>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue'
import type { DAGNodeDefinition } from '../../types/dag-node'
import { BASE_NODE_DEFINITIONS } from '../../types/dag-node'

const search = ref('')

const categories = ref([
  { name: '📥 数据源', expanded: true, items: BASE_NODE_DEFINITIONS.filter(d => d.category === 'source') },
  { name: '🔧 处理', expanded: true, items: BASE_NODE_DEFINITIONS.filter(d => d.category === 'process') },
  { name: '🤖 AI', expanded: true, items: BASE_NODE_DEFINITIONS.filter(d => d.category === 'ai') },
  { name: '📤 输出', expanded: true, items: BASE_NODE_DEFINITIONS.filter(d => d.category === 'output') },
])

const filteredCategories = computed(() => {
  if (!search.value) return categories.value
  const q = search.value.toLowerCase()
  return categories.value.map(c => ({
    ...c,
    items: c.items.filter(item =>
      item.label.toLowerCase().includes(q) || item.type.includes(q)
    ),
  })).filter(c => c.items.length > 0)
})

const onDragStart = (event: DragEvent, def: DAGNodeDefinition) => {
  event.dataTransfer?.setData('application/dag-node-type', def.type)
  event.dataTransfer!.effectAllowed = 'move'
}
</script>

<style scoped>
.node-library {
  width: 220px; height: 100%; background: #181825; border-right: 1px solid #313244;
  overflow-y: auto; user-select: none;
}
.library-search { padding: 8px; }
.search-input {
  width: 100%; padding: 6px 8px; background: #1e1e2e; border: 1px solid #313244;
  border-radius: 4px; color: #cdd6f4; font-size: 12px; outline: none;
}
.category-header {
  display: flex; align-items: center; gap: 6px; padding: 6px 10px;
  font-size: 12px; font-weight: 600; cursor: pointer; color: #a6adc8;
}
.category-header:hover { background: #1e1e2e; }
.library-item {
  display: flex; align-items: center; gap: 8px; padding: 6px 10px 6px 24px;
  font-size: 12px; cursor: grab; border-left: 3px solid transparent;
}
.library-item:hover { background: #1e1e2e; }
.library-item:active { cursor: grabbing; }
</style>
```

---

#### Task 2.3: 创建属性面板

**Objective:** 右侧面板，选中节点时显示参数表单 + 输入输出数据预览

**Files:**
- Create: `src/components/dag/PropertiesPanel.vue`

```vue
<template>
  <div class="properties-panel" v-if="selectedNode">
    <div class="panel-title">📋 节点属性</div>
    <div class="panel-section">
      <div class="section-label">节点名</div>
      <input v-model="selectedNode.label" class="prop-input" @change="updateLabel" />
    </div>

    <!-- 动态参数表单 -->
    <div class="panel-section" v-if="params.length">
      <div class="section-label">参数</div>
      <div v-for="param in params" :key="param.key" class="param-row">
        <label class="param-label">{{ param.key }}</label>
        <input
          v-if="param.type === 'string' || param.type === 'number'"
          v-model="param.value"
          class="prop-input"
          :type="param.type === 'number' ? 'number' : 'text'"
        />
        <select v-else-if="param.type === 'select'" v-model="param.value" class="prop-select">
          <option v-for="opt in param.options" :key="opt" :value="opt">{{ opt }}</option>
        </select>
      </div>
    </div>

    <!-- 输出预览 -->
    <div class="panel-section" v-if="nodeOutput">
      <div class="section-label">📤 输出预览</div>
      <pre class="output-preview">{{ JSON.stringify(nodeOutput, null, 2) }}</pre>
    </div>
  </div>

  <div class="properties-panel empty" v-else>
    <div class="empty-hint">选中节点查看属性</div>
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import { useDAGStore } from '../../stores/dag-store'
import type { DAGNode } from '../../types/dag-node'

const store = useDAGStore()

const props = defineProps<{ selectedNode: DAGNode | null }>()

const params = computed(() => {
  if (!props.selectedNode) return []
  const config = props.selectedNode.config
  return Object.entries(config).map(([key, value]) => ({
    key,
    value: String(value),
    type: typeof value === 'number' ? 'number' : 'string',
  }))
})

const nodeOutput = computed(() => props.selectedNode?.output)

const updateLabel = () => {
  // label is updated via v-model on the node
}
</script>

<style scoped>
.properties-panel {
  width: 260px; height: 100%; background: #181825; border-left: 1px solid #313244;
  padding: 12px; overflow-y: auto; color: #cdd6f4; font-size: 13px;
}
.properties-panel.empty { display: flex; align-items: center; justify-content: center; }
.empty-hint { color: #6c7086; font-size: 13px; }
.panel-title { font-weight: 700; font-size: 14px; margin-bottom: 12px; }
.section-label { font-size: 11px; font-weight: 600; color: #a6adc8; margin: 10px 0 4px; text-transform: uppercase; }
.prop-input, .prop-select {
  width: 100%; padding: 4px 8px; background: #1e1e2e; border: 1px solid #313244;
  border-radius: 4px; color: #cdd6f4; font-size: 12px; outline: none;
}
.param-row { margin-bottom: 6px; }
.param-label { display: block; font-size: 11px; margin-bottom: 2px; }
.output-preview {
  background: #11111b; padding: 8px; border-radius: 4px; font-size: 11px;
  max-height: 200px; overflow: auto; font-family: monospace; white-space: pre-wrap;
}
</style>
```

---

#### Task 2.4: 创建主编辑器页面

**Objective:** 组装三栏布局：节点库 + Vue Flow 画布 + 属性面板

**Files:**
- Create: `src/pages/DAGEditor.vue`

```vue
<template>
  <div class="dag-editor">
    <!-- 工具栏 -->
    <div class="toolbar">
      <span class="workflow-name">📁 工作流: {{ workflowName }}</span>
      <div class="toolbar-actions">
        <button class="btn" @click="runAll" :disabled="isRunning">▶ 运行</button>
        <button class="btn" @click="stopAll" :disabled="!isRunning">■ 停止</button>
        <button class="btn" @click="autoLayout">↻ 自动布局</button>
      </div>
    </div>

    <div class="editor-body">
      <!-- 左侧: 节点库 -->
      <NodeLibrary />

      <!-- 中央: 画布 -->
      <div
        class="canvas-wrapper"
        @drop="onDrop"
        @dragover.prevent
      >
        <VueFlow
          v-model="elements"
          :default-viewport="{ x: 0, y: 0, zoom: 1 }"
          :min-zoom="0.1"
          :max-zoom="4"
          :snap-to-grid="true"
          :snap-grid="[20, 20]"
          fit-view-on-init
          @node-click="onNodeClick"
          @pane-click="selectedNode = null"
          @connect="onConnect"
          @edge-click="onEdgeClick"
        >
          <Background :gap="20" />
          <Controls />
          <MiniMap
            class="dag-minimap"
            :pannable="true"
            :zoomable="true"
            node-stroke-color="#555"
          />

          <template #node-custom="nodeProps">
            <ComfyNode
              :id="nodeProps.id"
              :label="nodeProps.data.label"
              :icon="nodeProps.data.icon"
              :color="nodeProps.data.color"
              :status="nodeProps.data.status"
              :inputs="nodeProps.data.inputs"
              :outputs="nodeProps.data.outputs"
              :has-params="false"
              :selected="nodeProps.selected"
              :duration="nodeProps.data.duration"
            >
              <template #params>
                <!-- 参数将在后续任务中内联 -->
              </template>
            </ComfyNode>
          </template>
        </VueFlow>
      </div>

      <!-- 右侧: 属性面板 -->
      <PropertiesPanel :selected-node="selectedNode" />
    </div>

    <!-- 底部日志 -->
    <div class="console" v-show="logs.length">
      <div v-for="log in logs" :key="log.id" :class="['log-line', log.level]">
        {{ log.text }}
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue'
import { VueFlow, useVueFlow } from '@vue-flow/core'
import { Background } from '@vue-flow/background'
import { Controls } from '@vue-flow/controls'
import { MiniMap } from '@vue-flow/minimap'
import { useDAGStore } from '../stores/dag-store'
import { BASE_NODE_DEFINITIONS } from '../types/dag-node'
import type { DAGNode } from '../types/dag-node'
import ComfyNode from '../components/dag/ComfyNode.vue'
import NodeLibrary from '../components/dag/NodeLibrary.vue'
import PropertiesPanel from '../components/dag/PropertiesPanel.vue'

import '@vue-flow/core/dist/style.css'
import '@vue-flow/core/dist/theme-default.css'
import '@vue-flow/controls/dist/style.css'
import '@vue-flow/minimap/dist/style.css'

const store = useDAGStore()
const { addNodes, addEdges, removeEdges, fitView } = useVueFlow()

const workflowName = ref('未命名工作流')
const selectedNode = ref<DAGNode | null>(null)
const isRunning = ref(false)
const logs = ref<{ id: string; text: string; level: string }[]>([])

const elements = computed(() => store.nodes.map(n => ({
  id: n.id,
  type: 'custom',
  position: n.position,
  data: {
    label: n.label,
    icon: BASE_NODE_DEFINITIONS.find(d => d.type === n.type)?.icon || '📦',
    color: BASE_NODE_DEFINITIONS.find(d => d.type === n.type)?.color || '#6c7086',
    status: store.nodeStatuses[n.id] || 'idle',
    inputs: BASE_NODE_DEFINITIONS.find(d => d.type === n.type)?.inputs || [],
    outputs: BASE_NODE_DEFINITIONS.find(d => d.type === n.type)?.outputs || [],
    duration: n.duration,
  },
})))

// ─── 事件处理 ───
const onDrop = (event: DragEvent) => {
  const type = event.dataTransfer?.getData('application/dag-node-type')
  if (!type) return
  const def = BASE_NODE_DEFINITIONS.find(d => d.type === type)
  if (!def) return

  const bounds = (event.currentTarget as HTMLElement).getBoundingClientRect()
  store.addNode({
    id: `node_${Date.now()}`,
    type: def.type,
    label: def.label,
    position: { x: event.clientX - bounds.left - 130, y: event.clientY - bounds.top },
    config: { ...def.defaultConfig },
  })
}

const onNodeClick = ({ node }: any) => {
  selectedNode.value = store.nodes.find(n => n.id === node.id) || null
}

const onConnect = (connection: any) => {
  // Type-check: source output type must match target input type
  const sourceDef = BASE_NODE_DEFINITIONS.find(d => d.type === store.nodes.find(n => n.id === connection.source)?.type)
  const targetDef = BASE_NODE_DEFINITIONS.find(d => d.type === store.nodes.find(n => n.id === connection.target)?.type)
  const sourcePin = sourceDef?.outputs.find(p => p.id === connection.sourceHandle)
  const targetPin = targetDef?.inputs.find(p => p.id === connection.targetHandle)

  if (sourcePin && targetPin) {
    // Allow if types match, or if either is 'any'
    if (sourcePin.type === 'any' || targetPin.type === 'any' || sourcePin.type === targetPin.type) {
      store.addEdge({
        id: `edge_${connection.source}_${connection.sourceHandle}_${connection.target}_${connection.targetHandle}`,
        source: connection.source,
        target: connection.target,
        sourceHandle: connection.sourceHandle,
        targetHandle: connection.targetHandle,
      })
    } else {
      logs.value.push({ id: Date.now().toString(), text: `❌ 类型不匹配: ${sourcePin.type} → ${targetPin.type}`, level: 'error' })
    }
  }
}

const onEdgeClick = ({ edge }: any) => {
  store.removeEdge(edge.id)
}

// ─── 工具栏 ───
const runAll = () => {
  isRunning.value = true
  logs.value.push({ id: Date.now().toString(), text: '▶ 开始执行...', level: 'info' })
  // TODO: Phase 2 — invoke Tauri command
}

const stopAll = () => {
  isRunning.value = false
  logs.value.push({ id: Date.now().toString(), text: '■ 已停止', level: 'warn' })
}

const autoLayout = () => {
  fitView({ padding: 0.2 })
}
</script>

<style scoped>
.dag-editor {
  display: flex; flex-direction: column; height: 100vh; background: #11111b;
}
.toolbar {
  display: flex; align-items: center; justify-content: space-between;
  padding: 6px 16px; background: #181825; border-bottom: 1px solid #313244;
}
.workflow-name { font-size: 13px; color: #a6adc8; }
.toolbar-actions { display: flex; gap: 8px; }
.btn {
  padding: 4px 12px; border: 1px solid #313244; border-radius: 4px;
  background: #1e1e2e; color: #cdd6f4; font-size: 12px; cursor: pointer;
}
.btn:hover { background: #313244; }
.btn:disabled { opacity: 0.4; cursor: default; }
.editor-body { display: flex; flex: 1; overflow: hidden; }
.canvas-wrapper { flex: 1; position: relative; }
.console {
  height: 120px; background: #181825; border-top: 1px solid #313244;
  overflow-y: auto; padding: 8px 16px; font-family: monospace; font-size: 11px;
}
.log-line { padding: 2px 0; }
.log-line.info { color: #a6adc8; }
.log-line.error { color: #f38ba8; }
.log-line.warn { color: #f9e2af; }
</style>
```

---

#### Task 2.5: 注册新路由和 Vue Flow 组件

**Objective:** 在路由中添加 `/dag-editor`，注册自定义节点类型

**Files:**
- Modify: `src/router/index.ts`
- Modify: `src/main.ts`

**路由:**

```typescript
// src/router/index.ts — add:
{
  path: '/dag-editor',
  name: 'dag-editor',
  component: () => import('../pages/DAGEditor.vue'),
}
```

---

### Phase 1 验收标准

- [ ] 拖拽节点从左侧库到画布，正确渲染 ComfyUI 风格节点
- [ ] 节点显示状态灯、针脚（彩色圈）、参数区
- [ ] 连线类型安全：不同类型针脚拒绝连接
- [ ] 选中节点时右侧面板显示参数
- [ ] 画布支持缩放/平移（滚轮 + 拖拽）
- [ ] `npm run build` 无错误

---

## Phase 2: DAG 执行引擎 (P2)

> 目标：Rust 端 DAG 解析 + 节点执行 + Tauri events 实时状态推送

*详细任务待 Phase 1 完成后再写，核心方向：*
- `src-tauri/src/engine/dag.rs` — DAG 拓扑排序、依赖图构建
- `src-tauri/src/commands/dag_run.rs` — 执行命令，逐个节点调用现有 executor
- Tauri events: `node-status` (idle→running→success/error), `node-output`
- 前端监听 events，更新节点状态 + 连线动画

---

## Phase 3: 高级节点 (P3)

> 目标：网页抓取 + AI 节点 + 条件/循环 + 数据预览气泡

*详细任务后补*

---

## Phase 4: 增强 (P4)

> 目标：子图/模块化 + 工作流市场 + 导入/导出 + 性能

*详细任务后补*

---

> **下一步:** 若溪确认后，从 Task 1.1 开始执行。
