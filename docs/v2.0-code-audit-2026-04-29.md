# Workflow Engine Desktop v2.0 — 代码审计报告

> 日期: 2026-04-29 | 审计范围: 全项目 (`workflow-engine-desktop`)

## 一、代码度量

| 语言 | 文件数 | 代码行 | 占比 | 注释率 |
|------|--------|--------|------|--------|
| Rust | 66 | 8,256 | 61.6% | 6.1% |
| Vue | 16 | 2,939 | 22.0% | 3.6% |
| JSON | 14 | 1,755 | 13.1% | 0% |
| TypeScript | 16 | 1,019 | 7.6% | 4.6% |
| YAML | 8 | 524 | — | 已删除 |
| 其他 | 3 | 101 | 0.8% | — |
| **总计** | **126** | **14,594** | **100%** | **5.2%** |

> Rust/Vue/TS 核心源码 ~12.2K 行，体量适中。

## 二、架构评分

### 前端架构 ⭐⭐⭐⭐

```
Router (5 routes):
  / → Dashboard.vue          ← v1.x 仪表板 + 模板市场
  /editor/new → FlowEditor   ← v2.0 ComfyUI DAG 编辑器
  /editor/:id → FlowEditor   ← 加载已有工作流
  /history → RunHistory.vue  ← 执行历史
  /settings → Settings.vue   ← 系统设置
```

- ✅ 路由清晰，5 条无冗余
- ✅ v1.x Editor.vue 已删除，FlowEditor 为唯一编辑器
- ✅ 组件分层: `components/flow/` (v2.0 核心), `components/` (通用)
- ⚠️ 存在双重 store 体系: v1.x (`workflow.ts` + `editorStore.ts` + `executionStore.ts`) 与 v2.0 (`flowStore.ts`) 并存

### 后端架构 ⭐⭐⭐⭐⭐

```
src-tauri/src/
├── commands/    ← Tauri 命令层 (7 files): dag_run, pipeline, run, schedule, system, template, workflow
├── engine/      ← 执行引擎 (9 files): dag, dag_scheduler, scheduler, executor, parser, state, context, workflow, collect
├── nodes/       ← 节点执行器 (27 files): http, file, browser, excel, word, ai... + traits/registry
├── data/        ← 持久化 (5 files): db, models, config, paths
├── platform/    ← 平台抽象 (6 files): linux, windows, recording, traits
└── system/      ← 系统服务 (3 files): scheduler, tray
```

- ✅ 模块边界清晰，职责单一
- ✅ DAG 引擎与旧线性调度器共存但各司其职
- ✅ 平台抽象层 (traits) 设计合理

## 三、安全扫描 ⭐⭐⭐⭐⭐

| 检查项 | 结果 |
|--------|------|
| 硬编码密钥/密码 | ✅ 0 项 |
| SQL 注入 | ✅ 使用参数化查询 (rusqlite) |
| Shell 注入 | ✅ 0 项 |
| eval/exec 滥用 | ✅ 0 项 (仅 bundled deps) |
| debug 残留 | ✅ 0 项 (console.log/dbg!/eprintln!) |
| 硬编码路径 | ✅ 0 项 |

## 四、代码质量

| 维度 | 评分 | 细节 |
|------|------|------|
| Clippy (Rust) | ⭐⭐⭐⭐⭐ | 0 warnings |
| 测试覆盖 | ⭐⭐⭐ | Rust 19/20 pass; 前端 0 测试 |
| 前端 Lint | ⭐⭐⭐ | ESLint/Vitest 配置缺失 |
| 死代码 | ⭐⭐⭐ | 已清理 (本次审计删除 ~500KB) |
| 注释文档 | ⭐⭐⭐ | Rust 注释充分; 前端偏少 |

## 五、已执行的清理

| # | 项目 | 大小 | 原因 |
|---|------|------|------|
| 1 | `src/pages/TestPage.vue` | 6 行 | 未注册路由，死代码 |
| 2 | `src/dist/` | 484 KB | v1.x 旧构建产物 |
| 3 | `templates/*.yaml` (8 files) | 524 行 | v1.x 格式，已替换为 JSON |
| — | `src-tauri/target/` | 5.6 GB | ⚠️ 建议在 Windows 侧 `cargo clean` |

## 六、待改进项

### 🔴 高优先级

| # | 问题 | 建议 |
|---|------|------|
| 1 | Store 双重体系 | `workflow.ts` (combined) vs `workflowStore.ts` (clean) 定义了两个同名 Pinia store。Dashboard 导入了前者，executionStore 导入了后者。长期须统一为一个。 |
| 2 | `cargo test` 1/20 失败 | `test_map_node` 已知失败，需修复或标记 `#[ignore]` |

### 🟡 中优先级

| # | 问题 | 建议 |
|---|------|------|
| 3 | 前端 0 测试 | 至少为 `pinTypes.ts`、`flowStore.ts`、`useUndo.ts` 加单元测试 |
| 4 | ESLint 配置缺失 | 添加 `.eslintrc` + `vitest` 配置 |
| 5 | `.gitignore` 检查 | 确保 `src/dist/` 和 `target/` 已忽略 |

### 🟢 低优先级

| # | 问题 | 建议 |
|---|------|------|
| 6 | `stores/index.ts` 导出不一致 | 导出了 `useWorkflowStore` 从 `workflowStore.ts`，但 Dashboard 直接从 `workflow.ts` 导入 |
| 7 | 模板旧 JSON 缺失 `description` 顶层字段 | 不影响功能，但格式上可补充 |

## 七、UI/UX 检查

| 检查项 | 状态 | 备注 |
|--------|------|------|
| 键盘快捷键 | ✅ | Ctrl+Z/S/Delete/Escape 已接入 FlowEditor |
| 空画布提示 | ✅ | "从左侧拖节点到此处" |
| Toast 通知 | ✅ | useToast composable，已接入 App.vue |
| 撤销/重做 | ✅ | useUndo composable + Ctrl+Z/Ctrl+Shift+Z |
| 自动保存 | ✅ | useAutoSave 30s 间隔 + 崩溃恢复 |
| 深色主题 | ✅ | 全局 #0d1117 暗色系 |
| 加载状态 | ✅ | Dashboard loading state |
| 搜索/过滤 | ✅ | Dashboard 搜索 + NodePalette 搜索 |
| 响应式布局 | ✅ | 三栏编辑器自适应 |
| 中文错误提示 | ✅ | toast.error 均为中文 |
| 模板系统 | ✅ | 8 个 v2.0 DAG JSON 模板 |

## 八、总结

**总体评分: 4.2/5** ⭐⭐⭐⭐

> v2.0 代码基础扎实，Clippy 零警告、安全零漏洞、架构清晰。主要遗留问题是 v1.x/v2.0 store 双轨制需长期收敛，以及前端测试覆盖为零。

**建议优先做:**
1. `cargo clean` 释放 5.6GB（Windows 侧）
2. 统一 store 体系（合并 `workflow.ts` → `workflowStore.ts`）
3. 为核心模块加前端测试
